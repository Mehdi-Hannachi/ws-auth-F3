{
  "name": "authentification-f3",
  "version": "1.0.0",
  "description": "Authentification back and front ",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently  \"    cd client && npm start    \"        \"    cd server && npm run dev   \"   "
  },
  "author": "Hannachi Mehdi",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^6.0.0"
  }
}
